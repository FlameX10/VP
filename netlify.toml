# This file is in the VP folder - the parent netlify.toml should be used instead
# If deploying from this folder directly, use these settings:

[build]
  publish = "."
  
[[redirects]]
  from = "/sponsors"
  to = "/sponsors.html"
  status = 200

[[redirects]]
  from = "/gallery"
  to = "/gallery.html"
  status = 200
